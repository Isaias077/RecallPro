/**
 * Atoms are the smallest possible components, such as buttons, inputs, or text labels.
 * They serve as the foundational building blocks of our interface.
 * 
 * This directory contains atomic components that are:
 * - Small, single-purpose components
 * - Reusable across the application
 * - Usually wrappers around Material UI components with custom styling
 * 
 * Examples include:
 * - Buttons
 * - Text fields
 * - Typography elements
 * - Icons
 * - Loading indicators
 */

// Export all atom components here for easy imports